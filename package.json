{
  "name": "git-stack",
  "version": "0.1.0",
  "private": true,
  "description": "Lightweight stacked PR manager built on git + gh CLI",
  "scripts": {
    "test": "bats test/"
  },
  "devDependencies": {
    "bats": "^1.13.0",
    "bats-assert": "^2.2.4",
    "bats-support": "^0.3.0"
  }
}
